
#Ignore thumbnails created by Windows
Thumbs.db

# Local plugin build output (scripts/build-woocommerce-zip.sh);
# released ZIPs live on the GitHub Release, not in-tree.
/dist/
#Ignore files built by Visual Studio
*.obj
*.exe
*.pdb
*.user
*.aps
*.pch
*.vspscc
*_i.c
*_p.c
*.ncb
*.suo
*.tlb
*.tlh
*.bak
*.cache
*.ilk
*.log
[Bb]in
[Dd]ebug*/
*.lib
*.sbr
obj/
[Rr]elease*/
# …except release-hashes/ which holds the docs-only README pointing at
# the per-tag manifests; the actual JSON manifests live on the GitHub
# Release page, not in-tree.
!release-hashes/
_ReSharper*/
[Tt]est[Rr]esult*
.vs/
.idea/
#Nuget packages folder
packages/

# Node.js (Tailwind CSS build)
node_modules/
package-lock.json
# …except the wallet's lockfile, which we *do* commit so WalletScrutiny
# (and any other reviewer) can reproduce the same Tailwind/Inter outputs.
!src/DigiByte.Web/package-lock.json

# Claude Code local worktrees — agent scratch space, not source
.claude/worktrees/

# Local scratch files
src/chainz-cookies.txt
